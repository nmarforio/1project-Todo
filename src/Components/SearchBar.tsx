import React from "react";

const SearchInput = () => {
  return (
    <form>
      <input type="input" placeholder="ingridients"></input>
      <button type="submit">TASTY!</button>
    </form>
  );
};

export default SearchInput;
